services:
  astro-app:
    build: .
    ports:
      - "4321:4321"
    volumes:
      - .:/app
      - /app/node_modules
      # - /app/.astro
    env_file:
      - .env
    stdin_open: true
    tty: true